# Тестовое задание — Авито

## Проблемы и их решения

### 1. OpenAPI.yml
- **Проблема:** Не было точного описания `AdminToken` и `UserToken`. При открытии `openapi.yml` в [Swagger Editor](https://editor.swagger.io) возникала ошибка.
- **Решение:** Добавлено описание `AdminToken` и `UserToken` в `securitySchemes` для корректной работы OpenAPI.

### 2. Определение администратора
- **Проблема:** В задании сказано, что администратор должен видеть текущие назначения через методы чтения, но не указано, как определить, что пользователь является администратором.
- **Решение:** Добавлено поле `isAdmin` в `TeamMembers` для однозначного определения роли администратора.

### 3. Определение администратора/обычного пользователя
- **Проблема:** В задании сказано, что администратор и обычный член команды должны использовать разные ручки
- **Решение:** В JWT токены добавил поле role(true/false), которого говорило о роли юзера

---

## 🚀 Запуск проекта

### 1. Клонировать репозиторий
```bash
git clone https://github.com/Parnishkaspb/avito.git
cd avito
```

### 2. Запустить docker файл
```bash
docker-compose up -d
```

---

# Makefile:
### 1. Генерация мок-данных в базе:
```bash 
make mockdata
```

### 2. Запустить проект:
```bash 
make start
```

---

## [!] Дополнительно:
### Добавил ручку /statistic: 

```
{
    "total_prs": Кол-во PR,
    "total_teams": Кол-во Команд,
    "teams": [
        {
            "TeamID": ID команды,
            "TeamName": Название команды,
            "AdminCount": Кол-во админов в команде,
            "PRParticipants": Кол-во участников команды, которые задействованы в PR
        }
    ]
}
```